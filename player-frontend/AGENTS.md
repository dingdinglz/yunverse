# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

修改页面前，必须加载design-taste-frontend skill

如果web-fetch工具无法使用的话，可以使用agent-browser skill查看文档，该命令需要运行在沙箱外。